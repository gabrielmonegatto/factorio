# Sessão 2026-07-10

## O que foi feito
- Skill **mover-projeto** criada, testada e instalada em `~/.claude/skills/mover-projeto/` (SKILL.md + references/exemplos.md + scripts/rename_claude_json_key.py).
- Nasceu de uma execução real do mesmo dia: mover `zoac-front` e `zoac-backend` para dentro de `/Volumes/KINGSTON/claude/Zoac/` preservando /resume, memória e ~/.claude.json.
- Entrevista registrada em `entrevistas/mover-projeto.md` (blocos 1/3/4 extraídos da execução real; gatilho, escopo, comportamento e nome confirmados com o Pedro via AskUserQuestion).
- Testes: script validado contra ~/.claude.json sintético (renomeia/no-op/recusa colisão); teste cego de plano em subagente cobriu 10/10 itens; teste de gatilho 7/7 frases.

## Decisões (+ porquê)
- Escopo AMPLO (varre launchd, cron, settings e memórias de projetos vizinhos, não só o Claude Code) — escolha do Pedro; referências externas esquecidas são exatamente o que ele não lembra sozinho.
- Comportamento mapear → mostrar plano → executar (não execução cega) — mudar caminho toca no ~/.claude.json inteiro; o mapa dá a chance de abortar antes do estrago.
- Parte mecânica arriscada virou script (`rename_claude_json_key.py`) em vez de prosa — "prosa para julgamento, código para mecânica"; sed no ~/.claude.json é o erro nº 1 a prevenir.

## Descobertas
- O par certo/errado mais valioso veio de um quase-erro real: colisão case-insensitive no APFS (`-...-Zoac` vs `-...-zoac` são o mesmo diretório) — invisível até acontecer.
- O teste cego de gatilho apontou blindagem extra: "mover repo pra outra conta do GitHub" quase dispara (repo ≈ projeto); exclusão explícita adicionada à description.

## Ficou pra próxima
- Nada novo desta sessão; pendências do funil da Juliana seguem as mesmas.
