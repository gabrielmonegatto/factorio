# Handoff — funil /go no ar (20/08/2026)

> Frente de origem: fábrica (rede de canais). Frente tocada: **apps/eternall/mananciall-site** (repo próprio).
> Autorização: Gabriel mandou "segue o fluxo pra colocar as coisas no ar".

## Estado atual (feito e VERIFICADO)

- `mananciall.org/go/<canal>?v=NNNN` **no ar** — rota Astro `src/pages/go/[...canal].ts`,
  commit `7a155f2` no repo do site. Deploy: worker `manancial-new` (Cloudflare).
- Legado `/go?s=yt&v=` (QRs dos vídeos publicados do Spurgeon) → tratado como
  `spurgeon`, `legacy=1`. Verificado: 302 pra `/collections/spurgeon-library`
  com UTMs (`utm_source=youtube`, `utm_medium=qr`, `utm_campaign=spurgeon`, `utm_content=NNNN`).
- Scans logados na tabela `go_scans` (migration `db/006_go_scans.sql`, aplicada
  no D1 remoto `mananciall-db`). Verificado com SELECT: 4 scans de teste, país BR.
- `moody` já mapeado no redirect (destino provisório `/` até existir a coletânea).

## Próximos passos

1. **Biolink por pregador** (decisão do Gabriel: máx 3 ofertas — combo, clássico,
   devocional). Primeiro pro Spurgeon: vira o destino no mapa `DESTINO` da rota.
2. Coletânea "The Best of D.L. Moody" na livraria ANTES do canal Moody lançar.
3. Painel simples de scans (SELECT por canal/dia — pode entrar no bi.mananciall.org).

## Minas

- Destino por canal mora NO CÓDIGO da rota (mapa `DESTINO`) de propósito: trocar
  destino é deploy de 1 linha e o QR impresso nunca muda. Não criar config externa.
- Insert do scan é best-effort (try/catch): scan perdido é aceitável, redirect
  quebrado não. Não "consertar" isso pra throw.
- Deploy do site: `pnpm run deploy` com `CLOUDFLARE_API_TOKEN` + `CLOUDFLARE_ACCOUNT_ID`
  no ambiente (valores no `.env` da fábrica). O wrangler.toml ainda tem comentário
  desatualizado sobre Vercel; o domínio já está 100% Cloudflare (Gabriel, 20/08).

## Credenciais/paths

- `.env` da fábrica → `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID` (nunca o valor aqui)
- D1: `mananciall-db` (`ddae6874-2058-4aa1-927e-0e7887e2cba6`), tabela `go_scans`
